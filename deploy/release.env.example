# Release identity
RELEASE_TAG=__RELEASE_TAG__
APP_IMAGE_REF=electricity-monitor:__RELEASE_TAG__
REDIS_IMAGE_REF=redis:8-alpine

# Stable container identity used by deploy/rollback
APP_CONTAINER_NAME=electricity-app
REDIS_CONTAINER_NAME=electricity-redis
COMPOSE_PROJECT_NAME=electricity-monitor
DOCKER_RESTART_POLICY=unless-stopped

# Published port
APP_BIND_ADDRESS=127.0.0.1
APP_HOST_PORT=11450
APP_CONTAINER_PORT=8000

# Runtime defaults
APP_ENV=production
APP__REDIS__HOST=redis
APP__REDIS__PORT=6379
APP__LOGGING__LEVEL=info
REDIS_MAX_MEMORY=128mb

# Required public runtime config
APP__CORS__ALLOWED_ORIGINS=
APP__QQ_BOT__API_URL=
APP__QQ_BOT__PUBLIC_QQ_NUMBER=
APP__PUBLIC_SITE__DOMAIN=
APP__PUBLIC_SITE__PORT=
APP__ADMIN__DEFAULT_QQ_NUMBER=

# Compose secrets source files (host paths relative to release/)
APP_DATABASE_PASSWORD_SECRET_FILE=./secrets/app_database_password
APP_JWT_SECRET_SECRET_FILE=./secrets/app_jwt_secret
APP_QQ_BOT_BEARER_TOKEN_SECRET_FILE=./secrets/app_qq_bot_bearer_token
APP_EMAIL_SMTP_PASSWORD_SECRET_FILE=./secrets/app_email_smtp_password

# Health check used by deploy.sh
DEPLOY_HEALTHCHECK_URL=http://127.0.0.1:11450/api/health
DEPLOY_HEALTHCHECK_RETRIES=20
DEPLOY_HEALTHCHECK_INTERVAL=3

# Optional database overrides
# APP__DATABASE__HOST=db.example.internal
# APP__DATABASE__PORT=5432
# APP__DATABASE__USERNAME=postgres
# APP__DATABASE__DATABASE=electricity_pro
