//! 数据库Schema定义
//!
//! 此文件由Diesel自动生成
//! 运行 `diesel migration run` 后会自动更新

// 示例表结构（实际应用时会被diesel migration生成的内容替换）

// diesel::table! {
//     users (id) {
//         id -> Uuid,
//         username -> Varchar,
//         email -> Varchar,
//         password_hash -> Varchar,
//         created_at -> Timestamp,
//         updated_at -> Timestamp,
//     }
// }
